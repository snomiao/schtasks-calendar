{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "cd packages/calendar && npm run dev",
    "serve": "npx serve data -l tcp://0.0.0.0:50302",
    "postversion": "subtree-sync",
    "subtree-sync": "subtree-sync"
  },
  "devDependencies": {
    "dotenv": "^14.2.0",
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
